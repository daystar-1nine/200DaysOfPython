# Project Rules

## Git & Commits
- Always ensure that all Git commits are authored by the `daystar-1nine` account.
- Before staging or committing, verify that:
  - `user.name` is set to `Suraj Sawant`
  - `user.email` is set to `223058890+daystar-1nine@users.noreply.github.com` (or `surajonenine@gmail.com`)
- Do not make commits under any other credentials or emails (such as `shrawani3007@gmail.com`).
