[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "day78_decision_tree_churn"
version = "1.0.0"
description = "Day 78: Customer Churn Decision Tree Engine"
readme = "README.md"
authors = [{ name = "Suraj Sawant", email = "surajonenine@gmail.com" }]
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "scikit-learn",
    "statsmodels",
    "matplotlib",
    "seaborn",
    "pytest"
]
