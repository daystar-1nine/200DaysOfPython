[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-pkg"
version = "0.1.0"
description = "Basic packaging demonstration project for Tasks 3, 4, 5"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0"
]

[project.scripts]
demo-cli = "demo_pkg.core:main"
